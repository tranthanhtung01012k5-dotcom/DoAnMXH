<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/notificationsFragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_primary"
    tools:context=".Notifications.NotificationsFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- TIÊU ĐỀ -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="14dp"
            android:text="Hoạt động"
            android:textColor="@color/text_primary"
            android:textSize="30sp"
            android:textStyle="bold" />

        <!-- TABS -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingBottom="14dp"
            android:scrollbars="none">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <Button
                    android:id="@+id/btnAll"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:text="Tất cả"
                    android:textAllCaps="false"
                    android:textColor="@drawable/tab_text_color"
                    android:background="@drawable/bg_thread_tab" />

                <Button
                    android:id="@+id/btnUnread"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:layout_marginStart="8dp"
                    android:background="@drawable/bg_thread_tab"
                    android:text="Chưa đọc"
                    android:textAllCaps="false"
                    android:textColor="@drawable/tab_text_color" />

                <Button
                    android:id="@+id/btnFollow"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:layout_marginStart="8dp"
                    android:text="Lượt theo dõi"
                    android:textAllCaps="false"
                    android:textColor="@drawable/tab_text_color"
                    android:background="@drawable/bg_thread_tab" />

                <Button
                    android:id="@+id/btnConversation"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:layout_marginStart="8dp"
                    android:text="Cuộc trò chuyện"
                    android:textAllCaps="false"
                    android:textColor="@drawable/tab_text_color"
                    android:background="@drawable/bg_thread_tab" />

                <Button
                    android:id="@+id/btnMention"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:layout_marginStart="8dp"
                    android:text="Lượt nhắc"
                    android:textAllCaps="false"
                    android:textColor="@drawable/tab_text_color"
                    android:background="@drawable/bg_thread_tab" />

                <Button
                    android:id="@+id/btnFollowing"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:layout_marginStart="8dp"
                    android:text="Người mà bạn theo dõi"
                    android:textAllCaps="false"
                    android:textColor="@drawable/tab_text_color"
                    android:background="@drawable/bg_thread_tab" />

                <Button
                    android:id="@+id/btnRepost"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:layout_marginStart="8dp"
                    android:text="Bài đăng lại"
                    android:textAllCaps="false"
                    android:textColor="@drawable/tab_text_color"
                    android:background="@drawable/bg_thread_tab" />

            </LinearLayout>

        </HorizontalScrollView>

        <!-- KHOẢNG CÁCH GIỮA TAB VÀ LIST -->
        <View
            android:layout_width="match_parent"
            android:layout_height="8dp"/>

        <!-- DANH SÁCH -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            tools:listitem="@layout/item_notification" />

        <!-- EMPTY -->
        <TextView
            android:id="@+id/tvEmpty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:padding="24dp"
            android:text="Chưa có gì để xem ở đây hihi 😊"
            android:textColor="#888888"
            android:textSize="16sp"
            android:visibility="gone" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>